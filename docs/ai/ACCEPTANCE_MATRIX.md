# BookSwap acceptance matrix

Last audited: 2026-07-14. Status values: Pass, Partial, Fail, Blocked, Not verified. “Pass” means the stated criterion was exercised with current evidence; it does not imply the whole subsystem passes. P0 is critical security, data-loss, or a completely broken core flow. P1 is launch-blocking product, functional, accessibility, or infrastructure work.

Evidence shorthand:

- Repo: direct source/migration inspection on commit d644ad2.
- Backend: bookswap-development ref uibatsbzjswmtdvdrlxj after eight migrations.
- Baseline: commands and four-viewport results in QA_EVIDENCE.md.

| ID | Subsystem | Objective acceptance criterion | Status | Verification method | Evidence | Severity |
| --- | --- | --- | --- | --- | --- | --- |
| AUTH-01 | Authentication | Sign-up, sign-in, sign-out, recovery, and session refresh work with safe user-facing errors. | Partial | Two-user browser journey plus Auth logs. | Login/reset UI and Auth hook exist; Playwright covers no authenticated journey. | P1 |
| AUTH-02 | Authentication | Every new Auth user creates exactly one safe public profile with non-admin, non-banned defaults. | Pass | Insert temporary Auth user in development, inspect profile, delete user, confirm cascade. | Backend test created Audit Test User with banned=false/is_admin=false, then left zero rows; trigger is security-definer with empty search_path and restricted execute ACL. | P1 |
| AUTH-03 | Authentication | Server mutations work against development with a server-only service-role/secret key and never expose it to browser bundles. | Blocked | Configure local secret out of Git; exercise protected routes and inspect client bundle. | lib/supabase.ts keeps service role server-side, but no development service-role key is available through the connector/local environment. | P1 |
| PROF-01 | Profiles | A user can view and update only name, phone, and city; public readers see only id, name, city, and created_at. | Partial | RLS/grant tests as anon, owner, other user; browser edit flow. | Direct public reads return exactly id/name/city/created_at; email and `*` return 401/42501; banned profiles disappear. Owner/other edit browser flow is still blocked by the missing secret. | P1 |
| PROF-02 | Seller profiles | Public seller page shows safe profile fields, eligible ratings, and paginated active/sold listings. | Fail | Route/API/browser test signed out and signed in. | No public seller profile/listings route exists. | P1 |
| LIST-01 | Listings | Seller can create, edit, change state, and delete only owned physical-copy listings; banned users cannot publish or mutate. | Partial | Seller/other/banned API tests plus browser journey. | Routes, schemas, ownership checks, constraints, and RLS exist; no live service-role journey. | P1 |
| LIST-02 | Listings | Listing supports sale, exchange, or both; sale requires positive AZN price and exchange captures desired books. | Fail | Schema constraints, API tests, create/edit/detail browser flow. | Current schema requires positive price and has no intention or desired-book model. | P1 |
| LIST-03 | Listings | Optional structured book fields remain optional while essential fields and copy condition/flaws are clear. | Fail | Schema/API/UI inspection and usability test. | Existing fields cover title, author, ISBN, category, condition, city, description; language, genre normalization, publisher, year, edition, cover, annotation/damage, negotiability, area, and handoff preferences are absent. | P1 |
| IMG-01 | Images | Authenticated owners upload only allowed image types, maximum 5 MB, into their own folder. | Partial | Storage policy/bucket inspection plus owner/other upload tests. | Public listing-images bucket has JPEG/PNG/WebP and 5 MB limit; insert/delete folder policies exist; live authenticated test blocked by secret setup. | P1 |
| IMG-02 | Images | Seller can replace/remove images and abandoned uploads are cleaned when create/edit fails. | Fail | Browser/API failure injection; Storage object comparison. | Edit form has no replace/remove flow; upload precedes create and no compensating cleanup is implemented. | P1 |
| IMG-03 | Images | Listing deletion removes owned objects safely and never deletes another user's objects. | Fail | Delete listing with shared/foreign path attempts; inspect Storage. | Storage folder ownership is enforced, but listing delete has no verified object cleanup. | P1 |
| CAT-01 | Catalog | Public home and catalog return eligible listings plus safe seller summaries without API errors. | Pass | GET /api/listings, detail API, direct Data API, banned-seller probe, and browser at all four viewports. | Catalog/detail return 200 with a real active fixture and exactly the safe seller fields; banned seller/listing return zero rows; production browser has no console/page errors. | P0 |
| SEARCH-01 | Search | Title/author/description/ISBN search returns relevant active results across Azerbaijani, Russian, and English text. | Partial | Seed multilingual corpus; API relevance and no-leak tests. | Generated simple-language tsvector and GIN/trigram indexes and a functioning API query exist; no multilingual relevance corpus has been exercised. | P1 |
| FILTER-01 | Filters | Category/genre, language, condition, city/region, intention, price, and sort compose predictably and persist in navigation. | Fail | Pairwise API tests and four-viewport browser checks. | Current functioning API supports category, city, max price, sort, and query only; genre, language, region, and intention are absent. | P1 |
| PAGE-01 | Pagination | Catalog and seller inventory use stable opaque cursor pagination with deterministic tie-breakers and no duplicates/gaps. | Fail | API contract tests across equal sort values and concurrent insert. | API uses limit up to 50 with no cursor or nextCursor. | P1 |
| FAV-01 | Favorites | User can add/remove/view own saved active/sold listings and cannot retrieve non-public copies through a favorite. | Pass | Route/helper review, unit test, authenticated RLS/Data API matrix, transition/ban/delete probes, signed-out browser flow. | Active/sold rows return safe sellers; draft/locked/transitioned/banned-seller rows do not. Invalid, spoofed, banned-user, and anon inserts fail; atomic trigger covers service-role races; deletion leaves no orphan. | P0 |
| SHELF-01 | Reader shelves | User can save titles separately from copies and maintain privacy-explicit owned/read/custom shelves and wanted titles. | Fail | Schema/API/browser privacy tests. | Only physical-copy favorites exist; no title identity, shelves, or wanted list. | P1 |
| EXCH-01 | Exchange matching | Eligible two-way offered/wanted matches are surfaced without private shelf, exact location, or contact disclosure. | Fail | Two-user fixtures, positive/negative/privacy tests, browser flow. | No exchange intention, wanted-title, or match model exists. | P1 |
| CHAT-01 | Messaging | Only actual listing buyer and seller can create a room; seller_id must equal the listing owner. | Pass | Direct role-level RLS/FK matrix plus route source/type verification. | Valid buyer/owner succeeds; wrong seller, spoofed buyer, self-room, inactive/missing listing, banned participant, anon, and third party fail. Composite FK blocks privileged mismatches; zero tautological policies remain. | P0 |
| CHAT-02 | Messaging | Only room members read/send messages; banned users cannot send; payloads use protected Realtime. | Pass | Buyer/seller/third-user/banned/anon RLS matrix, live three-client Realtime test, publication/policy inspection, source regression. | Buyer insert reaches buyer and seller Postgres Changes subscriptions; subscribed third user receives zero. Nonmember, spoofed sender, banned sender, and anon fail; no Broadcast code remains. | P0 |
| CHAT-03 | Unread state | Per-user unread counts and read markers are correct across devices and visible in navigation/messages. | Fail | Two-client message/read/reconnect test. | Messages have no per-user read marker; notifications do not establish room unread state. | P1 |
| NOTIF-01 | Notifications | Users see/mark only their notifications; required delivery is durable and failures observable. | Partial | RLS tests, message event, reconnect, failure injection. | RLS and column-only read update are correct; notification UI exists; void notifyUser can be lost in serverless and live flow is unverified. | P1 |
| REV-01 | Reviews | Only the eligible buyer reviews a sold interaction once; public display excludes unsafe/private data. | Partial | Eligible/ineligible/duplicate RLS and API tests. | RLS, constraints, API, and display exist; live multi-user flow not run. | P1 |
| REP-01 | Reporting | Active users report valid targets once per open case with bounded reason; users see only own reports. | Partial | RLS/API duplicate and invalid-target tests. | Policies, reason/status checks, partial unique index, API, and admin surface exist; end-to-end flow unverified. | P1 |
| MOD-01 | Moderation | Missing moderation provider is reported as unavailable, not as a successful “demo pass”; decisions are reviewable. | Fail | Remove provider config and submit unsafe/normal fixtures. | lib/moderation.ts reports demo text passed when moderation keys are absent. | P1 |
| ADMIN-01 | Admin authorization | Only verified, non-banned administrators access admin data/actions; client metadata cannot grant admin. | Partial | Non-admin/admin/banned/metadata-forgery API tests. | Server auth helpers and is_admin database field exist; full live matrix not run. | P0 |
| ADMIN-02 | Admin audit | Every moderation, ban, report, privacy, and appeal action creates immutable actor/target/action/reason/timestamp history. | Fail | Perform each action and query audit history. | No admin audit table or durable action history exists. | P1 |
| DB-01 | Supabase RLS | Every exposed public table has RLS and least-privilege policies; cross-account tests pass. | Partial | Catalog inspection, advisors, direct Data API, and role-level functional tests. | All 9 public tables have RLS; public/private profile, chat, and favorites cross-account/state/ban cases pass. Full table/route matrix remains blocked by the missing service secret; advisor has one Pro-only Auth setting warning. | P0 |
| DB-02 | Database integrity | Migrations apply in order with expected schema, constraints, indexes, triggers, and generated types. | Pass | Dev apply; catalog inspection; type generation; advisor checks. | Eight migrations applied; 9 public tables, 37 constraints, 30 indexes, restricted security-definer functions, Auth/favorite triggers, and generated relationship types verified. | P1 |
| STOR-01 | Storage security | Public reads are intentional; upload/delete are owner-folder scoped; replacement and lifecycle cleanup pass. | Partial | Bucket/policy inspection and cross-user object tests. | Bucket/policies are configured correctly at catalog level; functional and lifecycle tests remain. | P1 |
| SVC-01 | Service-role APIs | Every privileged route verifies user, role/ownership, ban state, target state, and returned visibility before querying/mutating. | Partial | Route-by-route source review plus adversarial API tests. | Catalog/chat-room/favorite/message defects are fixed with database backstops where applicable. Full authenticated route matrix remains blocked by the missing local secret. | P0 |
| L10N-01 | Azerbaijani localization | Default document/UI/legal/metadata/errors are natural Azerbaijani; AZN and local locations are consistent. | Fail | String inventory, lang/metadata inspection, browser review. | app/layout.tsx uses lang=en and most product UI is English; AZN/local cities exist. | P1 |
| RESP-01 | Responsive behavior | Core pages and navigation work without horizontal overflow at 1440x900, 1024x768, 390x844, 360x800. | Partial | Agent-browser viewport matrix and Playwright interaction tests. | Home/catalog/detail render meaningful 200 content with scroll width equal to viewport at all four sizes; broader authenticated/core-page reflow coverage remains. | P1 |
| A11Y-01 | Accessibility | Keyboard/focus, labels, landmarks, contrast, touch targets, 200% zoom/reflow, motion preferences, and status announcements meet WCAG 2.2 AA. | Partial | Accessibility tree/manual keyboard/zoom/screen-reader review at four viewports. | Focus-visible and reduced-motion foundations exist; catalog search/location/condition controls expose empty accessible names, 8–10px text remains, and the comprehensive audit is incomplete. | P1 |
| BROW-01 | Browser errors | Core signed-out and signed-in journeys produce no unexpected console error, failed request, or HTTP 4xx/5xx. | Partial | Capture console/request/response at four viewports. | Production signed-out catalog/detail are clean and APIs return 200 at all four sizes. Signed-in coverage is blocked by the secret; `next dev` has a React Refresh CSP warning not present in production. | P1 |
| SEO-01 | SEO | Azerbaijani metadata, canonical/OG, robots, sitemap, manifest, and indexable public listing/seller pages are correct. | Partial | Inspect generated metadata and production URLs; crawler checks. | robots/sitemap/manifest and base metadata exist; default English metadata and missing seller pages remain. | P1 |
| PERF-01 | Performance | Core routes meet agreed Web Vitals/bundle budgets and paginate data/images efficiently. | Fail | Lighthouse/Web Vitals on representative data; bundle and network analysis. | Build passes, but marketplace routes load about 208–217 kB first-load JS; large client surfaces, unoptimized images, and no pagination remain. | P1 |
| TEST-01 | Test coverage | Unit/integration/E2E cover happy, error, and authorization paths for launch-critical flows. | Fail | Coverage map against this matrix. | 11 unit tests and 4 Playwright tests pass; direct RLS/live Realtime matrices cover chat/favorites, but protected route, image lifecycle, exchange, admin audit, accessibility, and broader failure paths remain. | P1 |
| DEPLOY-01 | Deployment readiness | Production backend/domain/secrets/legal/monitoring/backups/rate limits/rollback are configured and smoke-tested without dev dependencies. | Fail | Deployment checklist, environment audit, production-like smoke and rollback drill. | Build passes; only a dev backend is established; service secret, operator/legal decisions, durable rate limiting, monitoring, production target, and rollback evidence are absent. | P1 |

## Completion gate

Goal mode may start from this matrix, but BookSwap is not product-ready or launch-ready. Completion requires every launch-required row above to be Pass, no unresolved P0/P1 issue, and current evidence linked from QA_EVIDENCE.md.
