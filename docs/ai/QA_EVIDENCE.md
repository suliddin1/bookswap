# QA evidence

Evidence date: 2026-07-24 (Asia/Baku). Repository: D:\Codex Projects\2HandedBook, branch autonomous/bookswap-product, current slice base e8ec075.

No production database, deployment, remote branch, or protected secondary checkout was touched. The current private account response-boundary slice made no database/Auth/Storage/provider/external mutation; the authorized development project remains in the clean state recorded by the query-plan slice. No credential was written to tracked files. Transient server logs, captures, and Playwright output are created only under ignored test-results and removed before checkpointing.

## P1 private account response-boundary coverage - current evidence

Before this slice, authenticated profile, privacy-request, and favorites clients trusted successful JSON before committing render state. A malformed dashboard could make `profile.name.trim` or listing array operations throw; an invalid privacy timestamp could throw inside deterministic Baku date formatting; and a non-array favorites payload could silently render a false empty state. Profile and privacy mutations also assigned unvalidated successful records. Existing evidence covered signed-out boundaries and representative successful fixture UI, but not these malformed-success conditions.

The new `account-responses` boundary validates profile name/phone/city, a non-negative integral favorite count, fully validated owned/favorite listings, finite privacy timestamps, and the database-constrained privacy request type/status sets. Profile dashboard loads, profile saves, listing-status mutations, privacy history/submission, and favorites loads parse before state changes. It reuses the public marketplace listing validator, so private and public listing consumers cannot drift into different render-safety contracts. Rejected data reaches the existing Azerbaijani profile/privacy/favorites failure copy; diagnostic fields and invalid values remain absent from presentation. No route, response, Auth, ownership, RLS, or wire contract changed.

Permanent Vitest coverage accepts representative dashboard/profile/listing/favorites/privacy data and rejects an object in place of a listing array, a string count, numeric phone, non-array favorites, invalid timestamp, and unknown privacy status. The authenticated 320x800 browser regression sends malformed 200 bodies to `/api/profile`, `/api/privacy-requests`, and `/api/favorites`; all three settle into localized failure states, no provider diagnostic or invalid timestamp is rendered, base/200% widths remain exact, and console/page errors are zero.

The optimized-production matrix repeats profile, user-rights/privacy history, and favorites failures at 1440x900, 1024x768, 390x844, and 360x800. All 12 cases retain `lang=az`, exactly one `main` and `h1`, meaningful localized content, reduced motion, exact base/200% widths, exactly one expected account API request, and zero console/page/unexpected-request/HTTP/hydration/overlay failures. Five exact GET requests carrying Next's `rsc: 1` header ended in `net::ERR_ABORTED` prefetch cancellation with no response or console effect and are classified separately. Four representative 200%-text captures were visually inspected after normalizing scroll position and preserve readable recovery content and the bookstore layout.

Adversarial full-suite scheduling exposed a real focus race in privacy history: `loadError` could update in the catch step while `loaded` remained false, so the focus effect ran before its alert mounted; the later `loaded` update did not retrigger it. The focus effect now requires and depends on both `loaded` and `loadError`. The targeted optimized-production regression and unchanged full four-worker suite then pass.

The complete gate is lint pass; strict TypeScript pass; Vitest 45/45; optimized Next.js build pass with 38 generated pages; gzip budgets pass unchanged at shared 100.1/105 KiB, home 184.8/195, catalog 187.6/195, detail 188.8/200, and seller 185.4/195; Playwright 22/22 passes with four workers. No backend, Auth, RLS, database, privacy/favorite/listing row, Storage, environment, provider, remote, deployment, or protected-checkout mutation occurred. This is authenticated fixture UI/runtime evidence only; the wrong-project public configuration and missing service secret still block truthful protected-route authorization evidence.

## P1 public marketplace failure-state coverage - current evidence

Before this slice, home, catalog, listing-detail, and seller-storefront clients called `response.json()` and then trusted successful response bodies through optional access or direct state assignment. Non-OK and invalid-JSON failures generally reached localized catch paths, but a 200 response with malformed `items`, `seller`, listing, review, cursor, numeric, image, or timestamp fields could reach array/render/date operations and become a client exception or invalid presentation. The suite had no deterministic production-browser regression for these four public failure states.

The shared `marketplace-responses` boundary now accepts only complete listing-page, listing-detail/review, and seller-page shapes, including finite numeric values, valid listing states, string image arrays, finite seller/review counts, bounded review ratings, nullable review authors, opaque string-or-null cursors, and finite seller/listing/review timestamps. Catalog initial/load-more, the shared home request, listing detail, and seller initial/load-more all parse before committing state. Rejected success bodies, invalid JSON, and non-OK responses use the existing Azerbaijani unavailable or load-more copy; raw provider bodies, parser messages, and invalid values never reach presentation. API response shapes and successful pagination behavior are unchanged.

Permanent Vitest coverage accepts representative valid page/detail/seller responses and rejects a non-array page, out-of-range review, and invalid seller timestamp. Permanent Playwright coverage injects a plaintext 503 on home, a malformed successful catalog page, invalid JSON on detail, and a malformed successful seller summary at 320x800. All four settle into their named localized states, raw diagnostics remain absent, detail's parallel catalog request is accounted for without imposing scheduling order, document width remains exact at base/200% text, and console/page errors are zero.

The optimized-production matrix repeats those four scenarios at 1440x900, 1024x768, 390x844, and 360x800: 16/16 cases keep `lang=az`, exactly one `main` and `h1`, meaningful localized recovery content, reduced motion, exact base/200% document widths, and zero console/page/unexpected-request/hydration/overlay failures. Each home case records exactly its injected `503 /api/listings`; every other HTTP failure count is zero, and every API request matches the scenario. Four representative 200%-text captures were visually inspected across route/viewport combinations and preserve the bookstore layout with readable recoverable error states.

The complete gate is lint pass; strict TypeScript pass; Vitest 44/44; optimized Next.js build pass with 38 generated pages; gzip budgets pass at shared 100.1/105 KiB, home 184.8/195, catalog 187.6/195, detail 188.8/200, and seller 185.4/195; Playwright 21/21 passes with four workers. No backend, Auth, Storage, database, schema, environment, provider, remote, deployment, or protected-checkout mutation occurred. The server is stopped and all temporary scripts, captures, results, and traces are removed before checkpointing. This closes one public failure-injection sub-slice of P1-016; it does not claim protected authorization, exchange, or multilingual-search coverage.

## P1 privacy-minimized marketplace Web Vitals RUM - current evidence

The application now has a production-capable, provider-neutral field-measurement path without activating an external analytics service. A tiny client island uses Next's stable `useReportWebVitals` callback and is enabled only when the server-side `WEB_VITALS_ENABLED=true` setting was present at build. It accepts only LCP, CLS, and INP from `/`, `/listings`, UUID listing detail, and UUID seller storefront paths, replacing dynamic paths with `home`, `catalog`, `listing-detail`, or `seller-storefront`. Private routes, listing authoring/editing, and invalid paths return no payload. Client payloads contain exactly version, metric name, numeric value, route group, and navigation type; metric IDs, raw/dynamic URLs, query strings, performance entries/attribution, referrers, users, sessions, device/network/geography, and user-authored content are absent.

`POST /api/vitals` is independently inert unless the same server-side setting is present at runtime. When enabled it permits same-origin browser context, including a forwarded public host/protocol behind an internal Next runtime URL; requires JSON; limits the body to 1,024 bytes; strictly validates metric/value/route/navigation combinations; rejects extra identifiers; applies the existing 120-request/minute in-process boundary; and emits a no-store 204 after writing one structured `bookswap.web_vital` record. The record adds only the threshold rating (`good`, `needs-improvement`, or `poor`) needed for operations. No IP, request header, URL, or reader data is included in application-authored logs. This is an aggregation-ready log contract, not a claim that durable production retention or an approved provider already exists.

The first enabled optimized-production matrix uncovered a real proxy-boundary defect: comparing browser Origin with Next's internally reconstructed request URL rejected every valid beacon as 403 when public/internal hostnames differed. Verification now trusts the browser-controlled `Sec-Fetch-Site: same-origin` boundary first and otherwise compares Origin with the forwarded public host/protocol; a focused proxy test and the full rerun pass. Cross-site context returns 403, extra fields return 400, a 1,025-byte body returns 413, disabled mode returns 204 without logging, and no invalid probe creates a metric record.

The final enabled matrix covered home, catalog, listing detail, and seller storefront at 1440x900, 1024x768, 390x844, and 360x800. All 16 cases rendered meaningful Azerbaijani content with exact base and 200% document widths, reduced motion, and zero console/page/unexpected-request/HTTP/hydration/overlay failures. Each case produced LCP, CLS, and INP: 48 structured server events total, 12 per route group, with the exact seven log fields and no identifiers. Four 200%-text home captures were visually inspected and preserved the established bookstore layout/reflow. This local traffic proves instrumentation, transport, filtering, and logging behavior only; it is not representative field data and is excluded from p75 claims.

The complete enabled gate is lint pass; strict TypeScript pass; Vitest 43/43; optimized Next.js build pass with 38 generated pages including `/api/vitals`; all five manifest gzip budgets pass unchanged at shared 100.1/105 KiB, home 184.2/195, catalog 186.4/195, detail 187.3/200, and seller 184.8/195; Playwright 20/20 passes with four workers. DR-007/DR-008 still block production target, log/metrics ownership, retention, and activation. Representative production traffic segmented mobile/desktop must then meet p75 LCP <=2.5s, CLS <=0.1, and INP <=200ms. PERF-01 remains Partial and P1-015's field-evidence remainder is blocked rather than fabricated.

## P1 representative marketplace query plans - current evidence

The initial 60,000-listing/200-seller/9,000-review development replay exposed two defects that empty-table inspection could not. First, PostgREST's disjunctive equality/tie cursor was a filter rather than a composite index condition: the newest deep page discarded 21,603 rows and touched 43,710 shared buffers for 25 results (approximately 231ms), while price cursors discarded about 22,500 rows and touched about 45,000 buffers (approximately 45-61ms). Splitting the predicates proved row-value/index conditions were the needed shape. Second, executing the direct table query as the real anonymous role showed the listing RLS security barrier correctly preserved seller-ban visibility but prevented numeric/full-text predicates from reaching their indexes: price/search paths discarded 43,470-49,304 rows and took approximately 313-359ms.

The public APIs now call fixed pagination readers. The privileged implementations live only in the non-exposed `private` schema with `search_path=''`; exposed `public` functions are stable `SECURITY INVOKER` wrappers with execute granted only to `anon` and `authenticated`. The private catalog reader validates query/sort/limit/cursor bounds, uses `simple` web-search configuration, row-value newest/price cursor predicates, active-only status, and an explicit non-banned seller join. The seller reader validates its seller/newest cursor, returns only that non-banned seller's active/sold inventory, and forces a custom plan. Direct table RLS remains unchanged as defense in depth. Anonymous probes confirm catalog status isolation, banned-seller exclusion in both readers, seller/status scope, representative price/search results, and rejection of a 52-row page request and explicit null sort. Public wrappers are non-privileged, private implementations are stable owner-controlled functions, generated RPC types are present, and the security advisor returns zero findings.

The version-controlled `supabase/tests/marketplace_query_plans.sql` probe uses deterministic fixtures inside an explicit transaction, captures `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`, asserts required index names/no avoidable ordered sort/no more than 100 post-index discards/non-empty results, exercises the anonymous RPC authorization contract, removes every fixture, re-analyzes the restored tables, commits cleanup, and only then returns evidence. Final warm plans at 60,000 listings use `listings_active_created_idx`, `listings_active_price_id_idx`, `listings_search_idx`, `listings_filters_idx`, `listings_public_seller_created_idx`, `reviews_listing_id_author_id_key`, and user primary-key lookup as intended. Newest/price/seller cursor paths discard zero rows, touch 18-65 shared buffers, avoid explicit sorts, and execute in approximately 0.09-1.04ms; rare search is approximately 4.44ms, combined filters 1.51ms, and sold-review join 0.74ms. These are representative development plans, not production latency promises.

The optimized-production browser matrix exercised fixture-backed catalog and seller routes at 1440x900, 1024x768, 390x844, and 360x800. All eight cases have `lang=az`, exactly one `h1` and `main`, meaningful route content, reduced motion, exact base and 200% widths, and zero console/page/unexpected-request/HTTP/hydration/overlay failures. Representative desktop, tablet, and mobile captures were visually inspected. A later full-suite run exposed a four-pixel 320px/200% overflow in the generic catalog empty/error heading; explicit full-width, zero-minimum, emergency wrapping fixed it, and the same complete suite then passed. The final gate is lint pass; strict TypeScript pass; Vitest 40/40; optimized Next.js build pass with 37 routes; all five manifest gzip budgets pass; Playwright 20/20. `git diff --check`, database cleanup, artifact cleanup, and port cleanup pass. PERF-01 remains Partial only because production-capable RUM/field p75 LCP, CLS, and INP evidence is still absent.

## P1 marketplace performance baseline, delivery, and budgets - current evidence

The untouched checkpoint build generated 37 routes with 102 kB shared first-load JavaScript and representative route totals of 225 kB home, 225 kB catalog, 231 kB listing detail, and 223 kB seller storefront. Its only two Framer Motion imports produced a 38.9 KiB gzip route chunk for decorative card/reveal movement. The full home route was client-owned, and all eight representative persisted covers were eager raw Supabase Storage `<img>` requests with no responsive candidates at each required viewport.

The current implementation removes Framer Motion, keeps decorative motion in CSS under the existing reduced-motion override, and makes the home editorial shell server-rendered. Featured and recent marketplace shelves are separate small client islands sharing one cached pending `/api/listings` request. Geometry-matched home/catalog fallbacks preserve the warm shelf structure; the route-level catalog fallback retains its real heading during streamed navigation. Persisted covers pass through `next/image` under the exact `https://*.supabase.co/storage/v1/object/public/listing-images/**` boundary, with explicit responsive sizes, quality 72, lazy loading by default, and priority only for the detail hero. Detail thumbnails use quality 65; blob/data authoring previews remain browser-local raw images because they cannot use the optimizer.

The final build remains 37 routes and 102 kB shared first-load JavaScript. Home is 189 kB, catalog 191 kB, listing detail 192 kB, and seller storefront 189 kB. The built-manifest gzip verifier passes shared 100.1/105 KiB, home 184.2/195, catalog 186.4/195, detail 187.3/200, and seller 184.8/195. Its route/dependency assertions are permanent unit coverage, and Playwright permanently verifies a representative persisted cover's lazy attribute, responsive `sizes`, and optimizer `srcset`.

The optimized-production browser matrix exercised home, catalog, fixture listing detail, and fixture seller at 1440x900, 1024x768, 390x844, and 360x800. All 16 cases have Azerbaijani document language, exactly one `h1` and `main`, exact base and 200% widths, no long reduced-motion animation, optimizer candidates for every persisted cover, observed requested widths only 96/256/640, no raw original Storage cover request, and zero console errors, page errors, unexpected request failures, HTTP failures, hydration text, or framework overlay. Home issues exactly one listings request despite two islands. Four representative full-page captures were visually inspected and retain the editorial bookstore hierarchy, readable cards/shelves, and clean desktop/tablet/mobile containment.

Synthetic lab diagnostics report approximate p75 LCP of 512ms home, 472ms catalog, 388ms detail, and 404ms seller, with maximum long tasks of 56–61ms. Catalog/detail/seller CLS is 0.0002–0.0616. Cold home streaming has a variable first-sample CLS around 0.12–0.20, while warm samples frequently reach 0.0002; this is explicitly unresolved and no synthetic sample is presented as field p75 evidence. Representative catalog/seller query plans now pass in the current slice. Production-capable RUM and field evidence remain required, so PERF-01 is Partial and P1-015 remains in progress.

Adversarial verification found two harness/runtime issues. Under full parallel load, soft catalog navigation could expose only the generic root loader before the segment resolved; a route-level catalog fallback now keeps the page heading and geometry, and the focused journey passes. Ten simultaneous Chromium workers intermittently saturated the local Next server, yielding client-exception screens and unrelated unstable interactions; all unchanged 20 tests passed at four workers in 16.4s and the normal wrapper passed 20/20 in 16.6s after the explicit four-worker cap. Assertions and coverage are unchanged.

Current complete gate: lint pass; strict `tsc --noEmit` pass; Vitest 40/40; optimized Next.js build pass with 37 routes; five manifest gzip budgets pass; Playwright 20/20. The current slice adds permanent cursor/RPC/plan/cleanup source regressions without changing client layout. `git diff --check`, changed-file UTF-8/mojibake scanning, artifact review, and port cleanup pass before checkpointing. The authorized development database received only the three additive marketplace-reader migrations and rollback-safe representative probes described above; no production, Auth, durable listing/message/notification row, Storage, provider, deployment, remote, or protected-checkout mutation occurred. The temporary matrix captures/logs and Playwright output are removed before checkpointing.

## P1 WCAG moderation and administrator-dashboard accessibility - current evidence

Administrator dashboard data, load failure, action reason, feedback, and pending state are owned by the exact authenticated user ID. An identity change synchronously hides prior data and clears local action state, aborts the prior load, and rejects stale load/action/refresh completion; repeated same-user Auth emissions do not restart the dashboard GET. Signed-out presentation starts no protected request. The dashboard response parser requires finite timestamps before deterministic Baku formatting, while malformed nested audit resolution time uses safe `Naməlum` presentation instead of allowing `RangeError` during render.

The page exposes one named local navigation model and eight focusable section headings. Overview statistics, audit entries, reader accounts, reports, privacy requests, and moderation decisions use list semantics. The listing moderation table retains its caption, scoped headers, and cell relationships inside a separately named, keyboard-focusable horizontal scroll region. Every action has entity context in its accessible name and references the visible immutable-history reason guidance. Actions remain operable with an empty reason so the textarea receives first-invalid focus, `aria-invalid`, and a stable help/error relationship. After a successful action removes its target, focused replacement status confirms the recorded mutation; a successful mutation whose refresh fails reports that split outcome accurately. Loading/fatal/error/busy/success states are explicit and focusable where necessary.

All essential administrator labels, hints, timestamps, status pills, diagnostics, identifiers, and action text use the shared 12px/contrast floor. Primary and repeated administrator actions meet the 44px target; other authored controls exceed the 24px AA minimum. Long Azerbaijani headings, user names/emails, report/privacy details, audit state, moderation metadata, and IDs wrap in zero-minimum containers; narrow action groups stack. At 320 CSS pixels with 200% root text the document remains exactly 320px wide, while the intrinsically wide data table remains fully available through its named two-dimensional region.

Adversarial validation caught and repaired three genuine runtime/presentation defects: nullable owner comparisons could dereference `null` when no user/state existed; arbitrary administrator and nested audit timestamps could throw during deterministic formatting; and the action-section heading expanded a 320px/200% document to 395px. It also repaired prior-account persistence, stale async settlement, generic action names, invalid reason-role/association behavior, post-action focus loss, duplicate table-region naming, weak 8–11px copy, undersized actions, and div-only collections. A pre-existing catalog geometry test now waits for Suspense completion before measuring, preserving the same 44px assertion under full parallel load.

Permanent coverage includes the signed-out no-request boundary; exact one-time authenticated dashboard load; named navigation/lists/table; stable report action payload; first-invalid reason focus/description; removal plus replacement focus; 44px action geometry; local-navigation focus; exact 320px base/200% width; top-level invalid timestamp rejection; and safe nested invalid resolution-time formatting. The complete repository gate is lint pass, strict TypeScript pass, Vitest 38/38, optimized Next.js build pass with 37 routes, and Playwright 19/19.

`agent-browser` was unavailable, so the repository-pinned Playwright Chromium runtime supplied the documented safe equivalent. The final fixture-backed optimized-production matrix exercised `/admin` at 1440x900, 1024x768, 390x844, and 360x800. Every case has `lang=az`, exactly one `h1` and `main`, meaningful content, named structures/controls, no authored target below 24px or administrator action below 44px, no essential direct text below 12px, no nested interaction, no long reduced-motion animation, exact base and 200% document widths, keyboard access to the table region, and visible three-pixel table/result/navigation focus. Console errors, page errors, unexpected failed requests, HTTP failures, hydration text, and framework overlays are zero. Four exact Next RSC prefetch `ERR_ABORTED` cancellations had no response or console effect and are classified separately. Visual inspection of all four full-page captures confirms readable desktop/tablet density, contained table navigation, clean mobile stacking, and preservation of the editorial bookstore hierarchy.

Auth, dashboard data, and representative report resolution were browser-local fixtures only. The preserved ignored public project-ref mismatch and missing development service secret remain the explicit unmocked authorization boundary; no backend success is claimed. No backend, Auth, administrator, report, privacy, listing, database, Storage, email, provider, schema, package, or external mutation occurred. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass after the final gate. The production server is stopped, port 3000 is free, and the temporary matrix script, four screenshots, traces, and result files are removed before checkpointing.

## P1 WCAG messaging and notification accessibility - current evidence

Messages, notifications, chat-room data, action feedback, errors, pending state, drafts, and the signed-in header's unread counts are now rendered only for their exact authenticated owner; room-chat state is additionally keyed to the room. Same-user Auth emissions do not restart protected loads. Detail responses must name the expected current user, stale request/Realtime/mutation completions cannot update another active context, and invalid reader/message/room/notification timestamps fail parsing before a deterministic formatter can throw. Existing API paths, message/notification wire values, atomic unread delivery, and protected Realtime design are unchanged.

The room list and notification collection are named and connected to their unread/action status. Load failures receive focus. Mark-all-read replacement feedback remains present, live, and visibly focused after its button disappears. The chat transcript is a named keyboard-scrollable `log` with polite additions, screen-reader sender prefixes, deterministic full-date labels on visible Baku times, and bottom-follow behavior that stops when a reader scrolls upward but resumes for an authored message. Enter submits, Shift+Enter inserts a line, stale send errors clear on edit, and the composer regains focus after the current-context request settles. Long reader names, listing titles, and messages wrap; header, cards, transcript, composer, feedback, timestamps, badges, hints, and actions meet the shared contrast, 12px essential-text, target, and constrained-reflow contracts.

The permanent authenticated 320x800 regression covers exact one-time room/notification loads under a repeated same-user Auth emission, named lists/log, sender context, deterministic timestamps, mark-all replacement focus, keyboard-scrollable transcript geometry, minimum listing-link/send targets, Enter versus Shift+Enter behavior, composer focus restoration, stable request IDs and API wires, and exact base/200% document width. Unit coverage rejects malformed room/message timestamps, and the notification boundary applies the same finite-parse guard, instead of permitting a render-time `RangeError`.

The final fixture-backed optimized-production matrix exercised `/messages`, `/notifications`, and `/chat/[roomId]` at 1440x900, 1024x768, 390x844, and 360x800. All twelve cases have `lang=az`, exactly one `h1` and `main`, meaningful content, named structures, no authored control below 24px or shared primary action below 44px, no essential direct text below 12px, no nested interaction, no long reduced-motion animation, exact base and 200% document width, and visible three-pixel focus. Console warnings/errors, page errors, unexpected failed requests, HTTP failures, hydration text, and framework overlays are zero. Twenty-two exact Next RSC navigation `ERR_ABORTED` cancellations had no response or console effect and are classified separately. Visual inspection of all twelve full-page captures is clean; after normalizing the capture scroll position, sticky-header stitching no longer obscures the evidence image.

Auth, Realtime, message/notification data, and representative PATCH/POST success were browser-local fixtures only. The preserved wrong-project public configuration and missing development service secret remain the explicit unmocked protected-route boundary; the earlier fresh-token `PGRST303` anomaly also remains unresolved. No backend, Auth, message, notification, database, Storage, email, provider, schema, package, or external mutation occurred. Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js optimized production build 37 routes; Playwright 17/17. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass. The production server is stopped, port 3000 is free, and the temporary matrix script, twelve screenshots, failure traces, and result directories are removed.

## P1 WCAG profile and privacy accessibility - current evidence

The profile dashboard now contributes no nested `main` beneath the shared page landmark. Its five visual sections are one named vertical tablist with selected state, roving focus, Arrow/Home/End activation, and one labelled tabpanel; each active section is independently named. Loading, fatal load, per-listing busy state, aggregate errors/notices, and profile-save success expose explicit status/focus behavior. Deleting a focused card or completing a listing action moves focus to the resulting feedback. Profile fields use localized first-invalid validation, `aria-invalid`, error relationships, input-purpose tokens, and phone privacy guidance; edits clear stale success. A user-ID-scoped load clears prior-account data before refetch, avoids duplicate same-user Auth refetch/abort, and remounts normalized uncontrolled field defaults after save.

The privacy-request form now supplies reviewed Azerbaijani minimum-length validation, first-invalid textarea focus, stable help/error relationships, busy state, and focusable atomic success/failure. Request loading/failure is named and focusable, and the history is one labelled list of semantic items and deterministic Baku dates. Profile/privacy essential labels, hints, actions, locations, dates, states, and feedback meet the shared 12px floor and contrast-safe tokens. Listing grids/actions, long reader/listing values, dashboard headings/cards, privacy form/history, and the authenticated header wrap or stack. The compact account menu includes sign-out.

Adversarial production evidence found and repaired genuine defects before acceptance. At 1440px/200% text, authenticated header controls expanded beyond the viewport, the continuous reader-name fixture expanded profile width to 2542px and squeezed `Elan yarat` to 42px, and user rights widened to 1845px. Privacy pills also inherited the global 11px size, while same-user Auth stabilization could abort the first protected GET. Header wrapping, zero-minimum/emergency name wrapping, non-shrinking action geometry, an explicit 12px pill override, and user-ID-scoped effects repaired the full set. The permanent 320x800 regression covers tab semantics/keyboard focus, one-main structure, signed-in compact-menu sign-out, same-user fetch stability, localized validation/error/success focus, history naming, target geometry, and exact base/200% width.

The final fixture-backed optimized-production matrix exercised `/profile` and `/user-rights` at 1440x900, 1024x768, 390x844, and 360x800. Every case has `lang=az`, exactly one `h1` and `main`, meaningful content, named controls, no authored control below 24px or shared primary action below 44px, no essential direct text below 12px, no nested interaction, no long reduced-motion animation, exact base and 200% document width, and a visible/unobscured three-pixel focus indicator. Console warnings/errors, page errors, unexpected failed requests, HTTP failures, hydration text, and framework overlays are zero. Eight exact Next RSC prefetch `ERR_ABORTED` cancellations had no failed response or console effect and are classified separately. Visual inspection of all eight full-page captures is clean and preserves the bookstore hierarchy, readable profile/listing actions, compact navigation, form guidance, and stacked mobile history.

Auth, Realtime, profile/privacy data, and representative PATCH/POST success were browser-local fixtures only. The preserved ignored public project-ref mismatch and missing development service secret remain the explicit unmocked protected-route boundary; the earlier fresh-token `PGRST303` anomaly also remains unresolved. No backend, Auth, profile, listing, privacy-request, database, Storage, email, provider, schema, package, or external mutation occurred. Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js optimized production build 37 routes; Playwright 16/16. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass. The production server is stopped, port 3000 is free, and the temporary matrix script, eight screenshots, failure traces, and result directories are removed.

## P1 WCAG listing-authoring and authentication accessibility - current evidence

The create-listing journey now exposes its four steps as one named ordered progress model with localized `n/4` context and current-step state. Advancing or returning focuses the inserted step heading; completion focuses the replacement heading. Silent disabled progression was replaced with explicit Azerbaijani validation that marks, describes, and focuses the first invalid title, author, description, photo, or price control. General upload/publish errors remain assertive and focusable. The step panel and publish action expose busy status, photo collections are named, the hidden file input gives its visible drop target the shared three-pixel focus indicator, and labels/hints/actions use the shared contrast-safe 12px floor and 44px target. Responsive progress connectors, card padding, long headings, actions, and preview content shrink, wrap, or stack.

Edit listing provides a named loading status, required description contract, named photo group, busy form state, focusable associated error feedback, visible file-picker focus, atomic save/cleanup status, 44px back/photo/save actions, and constrained fields/cards. Login/signup/recovery moves focus to each changed or completion heading, clears stale mode errors, hides decorative book-spine text from assistive technology, identifies busy state, focuses server/configuration errors, and supplies `name`, `email`, `current-password`, and `new-password` input-purpose tokens. Password guidance is visible and separately described so field names remain concise. Password reset associates the persistent minimum-length hint and dynamic error, marks/focuses the first invalid password control, names busy status, and focuses completion.

The permanent 320x800 regressions cover formerly silent authoring validation, current-step semantics and heading focus, condition/file target geometry, visible file focus, invalid-file association, edit photo naming and targets, Auth mode focus/input purpose, reset mismatch association/focus, and exact base/200% document width. The first full E2E pass exposed that a hint nested inside the password label enlarged the accessible name; moving described guidance outside the label repaired it. The final optimized-production matrix exercised create, fixture-backed edit, login/recovery, and reset at 1440x900, 1024x768, 390x844, and 360x800: all 16 cases have meaningful content, named controls, no sub-24px authored control or sub-44px shared primary action, no essential direct text below 12px, no nested interaction, no long reduced-motion animation, exact base and 200% widths, and zero console/page/request/HTTP/hydration/overlay failure. Representative desktop, tablet, and both mobile full-page captures preserve the warm bookstore hierarchy, visible focus, and readable error/status presentation.

The fixture intercept applies only to the edit listing GET and is UI/runtime evidence; no listing, image, Auth, email, database, or external mutation was submitted. A separate unmocked optimized-production listing-detail call returned the expected safe Azerbaijani 503 `INTERNAL_ERROR` caused by the preserved wrong-project local public configuration. The Playwright 1.60 browser cache had been absent after local package state changed and was restored outside the repository; package files are unchanged. Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js optimized production build 37 routes; Playwright 15/15. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass. The production server is stopped, port 3000 is free, and the temporary matrix script, sixteen screenshots, failure traces, and result directories are removed.

## P1 WCAG listing-detail and seller-action accessibility - current evidence

The public listing-detail journey now applies the shared measured palette and readability contract to every owned essential label, metadata value, seller detail, review/report instruction, and live result. The gallery is a named region/group; thumbnail buttons expose selected state; the favorite button exposes pressed state; and buttons declare explicit types. Loading supplies a semantic status plus one route heading. Review and report forms have visible labels, safe placeholder contrast, busy-disabled actions, status relationships, and atomic live feedback. The report disclosure is a 44px keyboard target with the shared three-pixel focus indicator. Back, seller, safety, favorite, message, review, and report actions meet the owned target contract without nested interactions.

Constrained enlargement testing found and repaired one genuine runtime layout defect before acceptance. At 320px with the root text size doubled, the unwrapped current/original price pair and the reviews/safety grid established min-content widths beyond the viewport. The price row now wraps; detail/review grid children have zero minimum width; long action copy, user titles, seller names, review text, ISBNs, and headings wrap without losing content; mobile metadata and seller information stack. The permanent regression now passes exact 320px width at base and 200% text size.

The optimized-production matrix exercised active listing/seller actions at 1440x900 and 1024x768 plus sold listing/review/report states at 390x844 and 360x800. Every case has `lang=az`, exactly one main `h1`, named controls, no target below 24px, no primary target below 44px, no essential direct text below 12px, no nested interaction, exact base and 200% document width, reduced-motion execution, and a visible three-pixel disclosure outline. Focused disclosure geometry remained below the sticky header and within the viewport. Signed-out review and report submission exposed two localized status messages without a mutation. Visual inspection of all four final scroll-top captures is clean.

Across the four fixture-backed cases, console warnings/errors, page errors, unexpected failed requests, HTTP failures, hydration text, and framework overlays are zero. Two exact Next RSC `ERR_ABORTED` prefetch cancellations occurred at 1440px and 1024px and none at either mobile size; they produced no failed response or console effect and are classified separately. The fixture intercepts only listing APIs and is UI/runtime evidence. A separate unmocked optimized-production detail request returned the expected local-configuration boundary: 503 `INTERNAL_ERROR` with safe Azerbaijani copy. The ignored public ref mismatch and missing development service secret remain unchanged, so no backend cleanliness or protected action success is claimed.

Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js optimized production build 37 routes; Playwright 14/14. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass. No database/schema/package/Auth/Storage/email/provider or other external mutation occurred. The production server is stopped, port 3000 is free, and the temporary matrix script, four screenshots, initial failed-launch output, traces, and result directories are removed.

## P1 WCAG public discovery foundation - current evidence

The shared shell now exposes a first-tab Azerbaijani skip link targeting a focusable `main` landmark. Common controls, links, summaries, and authored tabbable elements receive a visible three-pixel warm focus outline with sticky-header scroll margin. Primary and compact navigation identify the current page. Opening the compact menu moves focus to its first link; Escape closes it and restores the trigger. Header, footer, and public discovery actions use at least 44px primary targets. The catalog filter/results/category regions have programmatic headings, loading/result status, and named controls. Listing cards no longer nest the favorite button inside the listing link.

The warm palette was darkened without changing the visual identity. Browser assertions measure `#8f6213` on paper at 4.669:1, `#6b6254` on the footer at 4.503:1, and the `#95866f` input boundary on its light field at 3.491:1. Essential home/catalog labels and actions now use a 12px floor. The primary gradient uses two dark warm stops under light text. This is key-token evidence for the owned public slice, not a claim that every remaining route/color combination has completed contrast review.

Adversarial keyboard and resize testing exposed two genuine defects and drove the final repairs. First, the catalog's intrinsic-width category/sort content expanded past 320px when text was doubled; grid tracks, fields, category labels, and the mobile sort control now shrink/wrap, and the permanent regression passes at 320x800 with 200% root text. Second, representative home listing cards and long Azerbaijani section headings widened the 360px document at 200%; card links/metadata now constrain and wrap within zero-minimum grid tracks and headings allow emergency word wrapping. All four required viewports now preserve exact document width at base and 200% text size. Framer reveal/card effects consult the user's reduced-motion preference instead of relying only on CSS transition overrides.

The fixture-backed optimized-production matrix exercised home and catalog at 1440x900, 1024x768, 390x844, and 360x800. Across all eight cases: `lang=az`; exactly one `h1`; the skip/main contract and visible first-tab focus pass; mobile/tablet menu focus open/Escape restoration passes; every visible button/input/select/textarea/summary has a programmatic name and a target at least 24px in both dimensions; primary discovery controls are at least 44px; nested interactive counts are zero; reduced-motion animations longer than one millisecond are zero; base and 200% page widths match the viewport; and framework overlays, hydration errors, console warnings/errors, page errors, failed requests, and HTTP failures are zero. Visual inspection of all eight full-page captures is clean and preserves the warm bookstore hierarchy.

The unmocked production runtime was also checked before fixture isolation. Every public listing request returned the already-documented 500 because ignored `.env.local` points at Supabase ref `lnhublqrtkdrrafghvki` instead of authorized development ref `uibatsbzjswmtdvdrlxj`; Chromium consequently emitted the matching resource error. No code change can truthfully remove that external configuration boundary without the correct public configuration, and no backend cleanliness is claimed. The final local fixture intercepts only `/api/listings` and therefore provides UI/hydration/responsive evidence, not protected-route or backend success evidence.

Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js production build 37 routes; Playwright 13/13. `git diff --check`, tracked UTF-8/mojibake scanning, artifact review, and port cleanup pass. An initial Playwright launch without the repository-local browser path started no page; the complete rerun used the existing `.playwright` runtime. No database/schema/package/Auth/Storage/email/provider or other external mutation occurred. The production server is stopped, port 3000 is free, and the temporary harness, screenshots, failure traces, and result directories are removed.

## P1 Azerbaijani FAQ and safety guidance - current evidence

`AZ_COPY.faq` and `AZ_COPY.safety` now own the public route metadata, headings, introductions, questions/answers, safety sections, and related-page actions. The route components render that contract and no longer contain English metadata, `Help center`/`Trust & safety`, English condition names, dashboard actions, report/review terms, or scattered policy prose. Stable database/API condition and status values remain unchanged. Unit/source evidence asserts dictionary ownership, unique safety section IDs, the accepted no-funds/no-buyer-protection/no-guarantee/no-verification boundary, and exclusion of the unresolved minor-policy instruction.

The copy review removed two misleading policy edges. Chat history is now described only as potentially helpful when a complaint is investigated rather than as continuously moderated evidence. The age-specific meeting instruction was removed because DR-003 is unresolved. FAQ and safety both state that users arrange payment, meeting, handoff, and optional delivery; BookSwap does not hold funds, process payment, provide buyer protection, verify every claim/condition, guarantee delivery, insure the agreement, refund payment, or resolve the parties' dispute. Practical credential, inspection, public-meeting, reporting, bank, and emergency guidance remains risk reduction rather than a protection promise. No new payment, delivery, contact-disclosure, dispute, or age policy was invented.

The optimized-production browser journey opened FAQ, keyboard-focused and toggled the second native disclosure, navigated through the localized help action to safety, and verified localized title/description metadata, `lang=az`, exactly one `h1`, eight FAQ disclosures, four labelled safety sections, at least 44px disclosure targets, reduced-motion execution, no unresolved age text, no mojibake, and no framework overlay. At 1440x900, 1024x768, 390x844, and 360x800, both pages matched viewport width and all eight full-page captures were visually clean. Console warnings/errors, page errors, unexpected failed requests, and HTTP 4xx/5xx were zero. One exact Next RSC `ERR_ABORTED` link-prefetch cancellation occurred in each desktop/tablet context and none in mobile contexts; they had no failed response or console effect and were classified separately.

The first adversarial 200% text-resize pass exposed a genuine tablet header overflow: the full desktop navigation remained active at 1024px and its sign-in group extended past the viewport. The header now uses its compact menu below 1101px, long Azerbaijani information-page headings can break when space is constrained, and related-help actions wrap within their container. After rebuilding, 200% text resize passed at every matrix viewport with exact page width, one `h1`, and no overlay. Base 1024px visual review confirms the compact menu preserves the BookSwap header hierarchy.

Current repository gate: lint pass; strict TypeScript pass; Vitest 38/38; Next.js production build 37 routes; Playwright 10/10. `git diff --check`, tracked-file UTF-8/mojibake scanning, and ignored/untracked artifact review pass. No database/schema/package/Auth/Storage/email/provider mutation occurred. The production server is stopped, port 3000 is free, and the temporary browser harness, eight screenshots, and server logs are removed.

## P1 Azerbaijani API and optional notification email - current evidence

The shared API serializer now treats HTTP status and machine `code` as the stable contract and Azerbaijani prose as presentation. Every code currently produced by `ApiError` has a reviewed mapping. Known errors no longer serialize constructor/provider detail, unexpected 4xx/5xx responses use generic Azerbaijani copy, and Zod field details keep field keys while replacing schema prose with one generic Azerbaijani instruction. The dormant Auth action route preserves safe provider code/status fields while replacing provider messages through the existing Azerbaijani Auth map. Remaining code-authored English route messages were replaced; internal logs and identifiers remain unchanged.

Optional notification email now derives from the same type/event-aware presentation used in-app. Message and system subjects are Azerbaijani, HTML declares `lang=az`, user-authored previews are escaped, unknown system `message` prose is ignored in favor of the safe fallback, and known `listing.approved`/`listing.rejected` events render the reviewed body. Unit evidence includes an attempted HTML injection preview and a private-provider-detail system payload. No email provider invocation occurred. Already applied SQL-authored legacy notification prose was not changed because that requires an additive migration plus authorized development apply/advisor/type verification.

The optimized production API returned exact localized 400 `INVALID_FILTER`, 400 `INVALID_ID`, and 422 `VALIDATION_ERROR` responses; the validation body contained neither submitted `provider detail` nor Zod `Required` prose. The representative notification page passed at 1440x900, 1024x768, 390x844, and 360x800 with `lang=az`, one `h1`, private robots, exact page width, deterministic `14 iyl 2026, 22:05`, known-system and user-message presentation, no mojibake/framework overlay, and zero unexpected console/page/request/HTTP failures. Visual review of 1440px and 360px captures preserved the bookstore hierarchy and readable mobile cards/footer. Each isolated context produced one exact `/listings?_rsc=...` `ERR_ABORTED` link-prefetch cancellation; these had no failed response or console effect and were classified separately.

Auth, page API data, header counters, and Realtime were browser-local representatives only because the ignored local public project ref differs from the authorized development backend and no service secret is available. No database, Auth, Storage, protected-route, or email mutation occurred. Current repository gate: lint pass; strict TypeScript pass; Vitest 37/37; Next.js production build 37 routes; Playwright 10/10. `git diff --check` and the UTF-8/mojibake scan pass. The production server is stopped, port 3000 is free, and the temporary browser harness/screenshots/result directory are removed.

## P1 Azerbaijani administrator dashboard and actions - current evidence

The protected administrator surface now has Azerbaijani title/description metadata and `noindex,nofollow`. `AZ_COPY.admin` owns its access, loading, hero, statistics, reason guidance, action, immutable history, listing, account, report, privacy, moderation, empty, error, and security vocabulary. Stable database/API values remain unchanged and receive presentation labels for listing/privacy states, audit targets/actions/before-after state, moderation outcome/surface/content/provider/reason/category, and cities. Audit and moderation timestamps use the explicit `Asia/Baku` formatter rather than runtime-default locale output. Administrator database errors and self-ban failures return safe Azerbaijani messages with the same machine codes; the moderation email handoff now carries the stable `listing.approved`/`listing.rejected` event plus reviewed Azerbaijani body copy.

The client boundary no longer trusts broad `any` or raw response messages. `parseAdminDashboardResponse` requires the complete top-level collection shape plus every field rendered from listings, users, reports, privacy requests, moderation decisions, and audit history. Malformed representative data falls back to generic Azerbaijani copy without exposing parser/provider/database detail. Action errors use recognized machine codes; unexpected fetch/runtime failures use a generic fallback. Loading and signed-out states each expose exactly one `h1`; the reason field has visible help, length bounds, `aria-busy`, and live error/busy/success states. Table headers use `scope=col`, icon controls include the affected listing title in their accessible names, long account addresses wrap, and empty collections have explicit copy.

The optimized production runtime passed the representative administrator UI at 1440x900, 1024x768, 390x844, and 360x800. Each viewport had `lang=az`, one `h1`, private robots, localized headings/values, deterministic `14 iyl 2026, 22:05`, page scroll width equal to viewport width, contained table scrolling, at least 32x32 icon controls, no mojibake/framework overlay/hydration fault, and no unexpected console, request, or HTTP failure. Visual review covered 1440px and 360px captures. One browser-local approve success exposed the Azerbaijani live status; one browser-local 409 preserved `approve`, listing ID, and bounded reason wire values while exposing only `ADMIN_ACTION_CONFLICT`'s safe Azerbaijani message. The expected 409 resource console line and one exact Next RSC `ERR_ABORTED` prefetch cancellation were classified separately from unexpected failures. A signed-out 390px context started zero `/api/admin/**` requests, and a malformed 1024px dashboard body produced only the safe localized fallback.

Authenticated session, dashboard/API responses, and the Realtime connection were browser-local representatives only. The first external attempt reproduced the durable configuration boundary: the ignored local public project host did not resolve, while durable state authorizes a different development ref and no server secret is available. The final matrix isolated Auth/Realtime and admin data in-browser so it is UI/runtime evidence, not backend or protected-route success evidence. No database, Auth, Storage, email provider, or protected route mutation occurred.

Current repository gate: lint pass; strict TypeScript pass; Vitest 34/34; Next.js production build 37 routes; Playwright 9/9. `git diff --check` passes. No schema/package change was made. The production server is stopped, port 3000 is free, and temporary browser scripts, screenshots, logs, and Playwright result files are removed.

## P1 Azerbaijani messaging and notification journeys - current evidence

The unfinished recovery slice was the top declared iteration-log owner at `84fd1b9`: messages, room chat, unread presentation, notifications, their private metadata, safe client parsing/error mapping, and deterministic timestamps. The existing uncommitted changes were coherent with that scope; no unrelated tracked or untracked source work was present. Three stale ignored `audit-*.png` screenshots contradicted earlier cleanup notes and were removed without touching the repository-local Playwright browser runtime. The failed-launch `.last-run.json`, temporary matrix script, and four review screenshots were removed after verification.

The interrupted rendering defect was runtime-default timestamp presentation in the affected components: `toLocaleTimeString([])` and `toLocaleString()` select environment-dependent locale/ICU behavior, which is not a stable Azerbaijani contract across Next's Node render and Chromium. The repair routes all message, room, and notification timestamps through explicit `Asia/Baku` helpers that render `22:05` and `14 iyl 2026, 22:05`. Focused tests assert those exact values and reject reintroduction of either runtime-default locale call. The client boundary now parses unknown room/message payloads rather than trusting broad `any`, and notification presentation maps known message/listing-moderation payloads to reviewed Azerbaijani while preserving user-authored previews and using a safe fallback for unknown system data.

The optimized production runtime passed 12 representative authenticated UI cases: `/messages`, `/notifications`, and `/chat/[roomId]` at 1440x900, 1024x768, 390x844, and 360x800. Every case had `lang=az`, `dir=ltr`, exactly one `h1`, private robots, viewport width equal to scroll width, deterministic Baku time, no mojibake, no framework overlay, and zero unexpected console/page/request/HTTP failures. Visual review covered all four chat sizes; the desktop/tablet sidebar and mobile conversation/composer/footer remained readable and unclipped. The 390px interaction probe mapped one expected browser-local moderation 503 to safe Azerbaijani copy and confirmed the notification read-all success status. Signed-out Playwright coverage proves messages, notifications, and room chat send no protected reads.

Authenticated data, Auth, route responses, and Realtime were browser-local representatives only. No backend mutation was submitted and this is not protected-route success evidence. The ignored `.env.local` public URL points at ref `lnhublqrtkdrrafghvki`, while durable state names `uibatsbzjswmtdvdrlxj` as the authorized development project; it was preserved rather than silently repointed. Together with the absent development service secret and the earlier fresh-token `PGRST303` anomaly, this keeps real authenticated browser/network cleanliness blocked under P0-005/P1-012.

Current repository gate: lint pass; strict TypeScript pass; Vitest 33/33; Next.js production build 37 routes; Playwright 9/9. The UTF-8/mojibake scan and `git diff --check` pass. No database/schema/package change was made.

## P1 Azerbaijani profile and privacy-request journeys - current evidence

The typed localization contract now covers the profile dashboard, profile editor, listing-state actions, privacy-request form/history, and user-rights route. Profile responses are source-constrained to `name`, `phone`, and `city`; aggregate loading fails when any profile/listing/favorite query fails. Profile/privacy routes keep known structured errors and use safe unexpected-failure responses. Signed-out components do not start protected requests. Profile metadata is Azerbaijani and `noindex,nofollow`; the user-rights metadata and mixed terminology are centralized.

Current gate: lint pass; strict TypeScript pass; Vitest 31/31; Next.js production build 37 routes; Playwright 8/8. Representative profile/user-rights UI passed at 1440x900, 1024x768, 390x844, and 360x800 with `lang=az`, exactly one `h1`, viewport width equal to scroll width, localized values/dates/statuses, every profile tab exercised, no stale owned English, and no mojibake. A real development password session established authentication, while browser-only GET responses supplied profile/privacy data because the missing service secret still blocks protected Next-route execution; no mutation was submitted and this is not backend success evidence.

One fresh-token probe produced direct unread/notification PostgREST `PGRST303` (`JWT issued at future`) errors even though database and local UTC clocks matched when checked. The final clean UI-only matrix stubbed those unrelated counters and had zero unexpected console/page/request or HTTP errors; the token anomaly remains unresolved under authenticated browser coverage. The server/screenshots/logs were removed. Final development counts are zero for Auth users, identities, sessions, refresh tokens, public profiles, listings, and privacy requests.

## P1 Azerbaijani listing authoring and authentication - current evidence

The typed localization contract now owns listing create/edit steps, fields, hints, image controls, validation, preview/progress/success/cleanup states, login/signup/magic-link/recovery modes, password-update states, and safe client/API/Auth fallbacks. Category, condition, and city selects display Azerbaijani while their values remain `Fiction`, `Very good`, and `Baku`; user-entered book data remains unchanged. New/edit/login/reset pages expose Azerbaijani title/description metadata, exactly one `h1`, and `noindex,nofollow`. Recognized Supabase Auth and listing/upload machine codes map to reviewed Azerbaijani copy, while unknown provider/database messages use generic safe fallbacks.

Production interactions filled the four-step listing form, retained the wire values, exposed the selected `Çox yaxşı` condition through `aria-pressed`, attached a browser-local valid PNG, and reached the final `24,5 ₼` preview. The publish button was not clicked, so no upload or listing mutation occurred. A real public edit GET rendered a temporary Azerbaijani fixture plus localized price/city/category/condition/image controls. Login moved through login, signup, and recovery modes; one invalid-credential request intentionally returned Auth 400 and displayed only the localized safe error. No signup, magic-link, reset email, or password update was submitted. The reset page's local mismatch validation returned a localized alert without network mutation.

The clean production matrix covered new/edit/login/reset at 1440x900, 1024x768, 390x844, and 360x800: all 16 cases had `lang=az`, `dir=ltr`, one `h1`, private robots, correct metadata, zero stale owned English, overflow, mojibake, console/page/request failures, or HTTP 4xx/5xx. The separate expected-invalid-credential probe produced one expected Auth 400/browser resource message and no unexpected failure. Visual inspection covered final authoring preview desktop, edit tablet, recovery mode 390px, and password mismatch 360px.

Current gate: lint pass; TypeScript pass; Vitest 30/30; Next.js production build 37 routes; Playwright 7/7. One temporary Auth-backed seller/listing supplied the edit read; sessions/refresh tokens were cleared before identity/Auth deletion, and final Auth/public fixture counts are zero. Servers, screenshots, and logs were removed. Protected create/edit/upload mutation evidence remains blocked by P0-005's missing development service secret and is not claimed by this UI/localization slice.

## P1 Azerbaijani listing, seller, and favorites journeys - current evidence

The typed localization contract now owns listing-detail actions/fields/status, review/report/trust vocabulary, seller identity/inventory/count/date states, favorites auth/load/empty states, safe client error-code mapping, and the current route metadata. Stable English marketplace values remain unchanged in the database and API; public pages display their Azerbaijani labels while user-entered book/review content passes through. Listing and seller routes expose Azerbaijani generic title/description/Open Graph metadata and exact per-ID canonical URLs. Favorites is private, Azerbaijani, and `noindex,nofollow`. Dynamic content-derived listing/seller metadata, production-host confirmation, and sitemap discovery remain explicitly pending.

Cross-runtime helpers were hardened after production-browser evidence: monetary values now render deterministic Azerbaijani grouping/decimal separators plus non-breaking-space `₼`, and dates use explicit Azerbaijani month names at the Asia/Baku calendar boundary. This fixed Chromium's initial `17.5 ₼` and `M07` output while preserving Node's `14 iyl 2026` contract. Top-level signed-out/unavailable states use one semantic `h1`; review/report feedback uses status semantics and safe localized machine-code mappings rather than raw server prose.

Real development evidence used two temporary Auth-backed public profiles, one active listing, one sold listing, a completed buyer/seller room, and one eligible public review. No browser response was mocked. Production pages/API showed the active detail, sold notice/review/form, seller rating and both active/sold inventory entries, and signed-out favorites. The signed-out review and report buttons returned the localized authentication requirement without sending a mutation. At 1440x900, 1024x768, 390x844, and 360x800, all four routes had `lang=az`, `dir=ltr`, exactly one `h1`, required localized copy, mapped marketplace values, deterministic manat/month output, expected canonical/robots metadata, zero old owned-surface English, overflow, mojibake, console/page/request failures, or HTTP 4xx/5xx. Visual inspection covered active detail desktop, seller tablet, sold/review mobile, and favorites 360px.

`next dev` was intentionally not used for final evidence: React Refresh requires dynamic evaluation and the production CSP forbids `unsafe-eval`. The CSP was not weakened. The optimized production runtime is clean. Current gate: lint pass; TypeScript pass; Vitest 30/30; Next.js production build 37 routes; Playwright 5/5. Sessions/refresh tokens were cleared before Auth deletion; final Auth users, identities, sessions, refresh tokens, public users, listings, room/read-state, and review fixture counts are all zero. Servers, screenshots, and logs were removed.

## P1 Azerbaijani public discovery foundation - current evidence

The root product now declares `lang=az`, `dir=ltr`, Azerbaijani base/social metadata, `az_AZ` Open Graph locale, Azerbaijani web-manifest language/copy, and Latin Extended Fraunces/Manrope subsets. One typed `lib/i18n.ts` contract owns the current copy plus category/condition/city/status labels and az-AZ number/date formatting. English database/API/query values remain stable: the browser displays `Bədii ədəbiyyat` while its link and selected catalog state preserve `category=Fiction`. Unknown and user-entered content passes through. `LOCALIZATION_INVENTORY.md` accounts for every remaining page/component/API/email/database-notification/legal surface and keeps missing legal operator/contact facts explicitly blocked. A root canonical was deliberately not added because Next metadata inheritance could incorrectly canonicalize every child route to `/`; per-route canonical URLs remain explicit SEO work.

Header/footer/global loading/error/not-found states, the full signed-out home page, catalog filters/sort/pagination/empty/error states, cards, covers, accessible names, locations, conditions, categories, status, and money are Azerbaijani. API failures on this public journey surface localized safe errors rather than server prose. Unit/source checks verify `az`/`az-AZ`, representative labels, date output, exact non-breaking-space `₼` format, unknown-value pass-through, and removal of the replaced English copy. Playwright verifies document language/title, raw query preservation, localized navigation/headings, deterministic manat output, mobile menu, public safety/right routes, and security headers.

The first production browser matrix found a real React hydration 418: Node formatted `Intl` currency style as `200 ₼` while Chromium formatted it as `200 AZN`, changing catalog text after hydration. The formatter now localizes the number only and appends an explicit non-breaking-space manat symbol. The entire matrix was rebuilt and repeated in isolated route contexts. At 1440x900, 1024x768, 390x844, and 360x800, home and `listings?category=Fiction` show correct language/title/description, loaded fonts, localized selected values, `200 ₼-dək`, zero horizontal overflow, no mojibake/old owned-surface copy/overlay/hydration/page/console/HTTP 4xx/5xx, and clean visual hierarchy. Mobile navigation expands with Azerbaijani names and no overflow; scrolling the decorative shelf reveals five covers with three in-viewport. A cancelled duplicate `/_rsc` link prefetch has no failed response or console effect and direct catalog contexts are clean. A transient first-run development-host DNS failure did not recur on the isolated repeat. No mocks or database fixtures were used.

Current gate: lint pass; TypeScript pass; Vitest 30/30; Next.js production build 37 routes; Playwright 4/4. Build size remains 214 kB first-load JS for home/catalog, consistent with the prior 208-217 kB range. The production server/browser were closed and all screenshots/logs removed. L10N-01 and SEO-01 remain Partial because listing, account, chat, notification, admin, route-specific metadata, email, API, and reviewed legal coverage are still pending.

## P0 protected listing publication boundary - current evidence

The final P1-009 state-machine review found a stop-ship bypass in the existing listing edit route: PATCHing only `{status:"active"}` on a draft or sold listing created no moderation checks, so a seller could republish an administrator-rejected draft even while the required provider was unavailable. Authenticated also retained direct Data API INSERT/UPDATE/DELETE grants on `public.listings`, allowing clients to skip the protected server route entirely.

The route now derives a deterministic moderation plan from current state, requested state, changed text, current images, and requested images. Any transition from draft or sold into public active state reviews the final title/description and every final image before mutation. Active-to-active edits review changed text and only new images, while unchanged active edits avoid redundant provider work. Locked targets return `LISTING_LOCKED` before moderation/provider use. Creation continues to review final text and all images. The plan has focused fixtures for draft publication, sold relisting, and active incremental editing, and a source contract proves the protected route consumes it.

Applied additive development migration: `require_protected_listing_mutations`. Development now has 17 migrations, 13/13 RLS public tables, 61 constraints, and 47 indexes. Anon and authenticated retain SELECT but have no INSERT/UPDATE/DELETE privilege on listings. Service role has explicit SELECT/INSERT/UPDATE/DELETE for the verified server boundary. Each authenticated INSERT, UPDATE, and DELETE functional probe fails with SQLSTATE 42501 even when the statement would touch no row; a rollback-only service-role insert/update/delete succeeds. Existing ownership/active-user RLS policies remain defense in depth if grants are deliberately changed later. Generated types are unchanged and agree with the deployed schema. Security advisors are empty; performance output remains only unused-index INFO on the cleaned development dataset.

Browser evidence used one temporary confirmed development Auth user with a genuine password session. The product login form successfully navigated to `/profile`; Auth `/user` returned 200. The production seller dashboard then used a browser-only representative `/api/profile` response so the UI could be inspected without the unavailable Next server secret. At 1440x900, 1024x768, 390x844, and 360x800, active and sold cards rendered with Mark sold/Relist controls, viewport and scroll widths matched, no framework overlay appeared, and a clean reload produced zero console/page/HTTP 4xx/5xx failures. At 390px both transition targets measured 79x34 CSS pixels. No listing mutation was clicked, and the mocked profile response is not backend success evidence. The browser session was closed, Auth sessions were revoked, the Auth user/identity/public profile were deleted, and screenshots/logs were removed.

Current gate: lint pass; TypeScript pass; Vitest 28/28; Next.js production build 37 routes; Playwright 4/4. Final Auth/public/Storage/ledger fixture counts are zero. P0-005's missing development service secret still blocks direct execution of the protected PATCH route, so LIST-01/SVC-01 remain Partial even though the bypass is closed at source and direct-client layers.

## P1 transactional immutable administrator actions - current evidence

Applied additive development migration: `add_transactional_admin_audit`. Development now has 16 migrations, 13/13 RLS public tables, 61 constraints, and 47 indexes. `admin_audit_log` records an administrator identity/name snapshot, target kind/ID, bounded action/reason, before/after object state, and server timestamp. It intentionally has no direct insert path: anon/authenticated have no privileges plus an explicit false policy; service role has SELECT only; update/delete are rejected by an immutable trigger even for the table owner. Current Supabase guidance confirms that Data API grants are separate from RLS and that public security-definer functions must revoke default `PUBLIC` execution. All four action RPCs revoke `PUBLIC`/anon/authenticated execution, grant service role only, use `search_path=""`, and repeat an active, non-banned administrator lookup inside the transaction. The schema security advisor is empty; performance output is only unused-index INFO on the cleaned zero-row dataset.

Every administrator route requires a trimmed 10-1000 character reason and delegates the state change plus audit insert to one database transaction. Ban/unban rejects self-targets, administrator targets, missing users, and no-op transitions. Listing approve/reject writes the required SYSTEM notification in the same transaction; optional email is attempted separately and reported. Reports move only from open to resolved/dismissed. Privacy and appeal requests reject repeated/final transitions, set `resolved_at` on the server, and use distinct `privacy_request.*` or `appeal.*` audit actions. The dashboard now fails when any constituent query fails and returns the latest 100 administrator actions separately from automated moderation decisions.

Live rollback-only development evidence used temporary administrator, seller, reporter, listing, report, access-request, and appeal fixtures:

| Probe                | Result                                                                                                                                                                         |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| User actions         | Ban and unban produced `user.banned` and `user.unbanned`.                                                                                                                      |
| Listing actions      | Reject and approve produced two audit rows and exactly two atomic SYSTEM notifications.                                                                                        |
| Report action        | Open to resolved produced `report.resolved`.                                                                                                                                   |
| Privacy actions      | Open to in-progress to completed produced both audited transitions.                                                                                                            |
| Appeal action        | Open to rejected produced `appeal.rejected`, not a generic privacy action.                                                                                                     |
| History shape        | Exactly eight rows, correct actor snapshot, valid reasons, before/after state, target, action, and timestamp; privacy details were absent.                                     |
| Forced audit failure | An injected audit check failure returned 23514 and rolled back the preceding ban completely.                                                                                   |
| Invalid cases        | Self-ban, administrator target, missing target, short reason, banned actor, repeated report resolution, and reopening a final privacy request all failed without side effects. |
| Direct access        | Anon/authenticated SELECT, service INSERT/UPDATE/DELETE, and authenticated RPC execution were denied; service SELECT succeeded.                                                |
| Tamper               | Owner UPDATE and DELETE each raised SQLSTATE 55000; neither persisted.                                                                                                         |

Catalog verification shows the four RPCs are security-definer with empty search paths and execute ACLs limited to postgres/service_role; private helpers and the mutation-rejection trigger are postgres-only. Generated types contain the audit table and all four RPC signatures. A final query confirms zero rows/objects across Auth, all fixture-bearing public tables, the audit/moderation/cleanup ledgers, and Storage.

Current gate: lint pass; TypeScript pass; Vitest 26/26; Next.js production build 37 routes; Playwright 4/4. Agent Browser rendered the production admin component at 1440x900, 1024x768, 390x844, and 360x800 with a browser-only representative dashboard response: immutable history and its reason/state/actor/target were visible, the required reason enabled all applicable controls, accessible labels/help were present, action targets were at least 32x32 CSS pixels, viewport and scroll widths matched, and console/page errors and framework overlays were zero. This UI-only payload was not used as backend evidence and no action was submitted. The authenticated Next admin route remains honestly blocked by P0-005's unavailable development service secret.

## P1 fail-closed reviewable moderation — current evidence

Applied additive development migration: `add_reviewable_moderation_decisions`. Development now has 15 migrations, 12/12 RLS public tables, 54 constraints, and 43 indexes. The new ledger contains request/actor/target identifiers, surface, content type, provider, outcome, bounded reason/category diagnostics, provider decision ID, and timestamp; raw submitted text and image URLs have no columns and are not retained. Anon/authenticated have no privileges plus an explicit false policy. Service role has SELECT/INSERT only; direct UPDATE/DELETE is grant-denied. Generated types agree with the deployed columns and actor relationship. Schema security lint is empty; performance output is unused-index INFO on the zero-row development dataset.

The moderation adapter has three explicit outcomes: approved, rejected, and unavailable. Without `OPENAI_API_KEY`, normal text/image input returns `PROVIDER_NOT_CONFIGURED`; provider timeout, network failure, non-success status, rate limit, and malformed success bodies also return unavailable. Local marketplace rules may reject known unsafe text without a provider, but can never approve content when the provider is absent. The configured OpenAI adapter validates the response shape and uses the documented multimodal image input for `omni-moderation-latest`; no paid/provider request was made during this unconfigured test slice.

Publishing and messaging fail closed: unavailable becomes Azerbaijani `MODERATION_UNAVAILABLE`/HTTP 503, rejected becomes `CONTENT_REJECTED`/HTTP 422, and an audit-write failure becomes `MODERATION_AUDIT_UNAVAILABLE`/HTTP 503. Listing creation checks final text plus every image; edits check the merged final text and only newly added images. Chat verifies authentication and room membership before provider use. Listing edits verify ownership before provider use. The authenticated preflight route records text/image results and returns 503 if either required check is unavailable. The protected admin dashboard queries and displays the last 50 decisions while explaining that raw content is excluded.

Focused fixtures prove normal unconfigured input is unavailable, the unsafe local fixture is rejected, a valid mocked multimodal response is approved, malformed provider output is unavailable, ledger inserts exclude the submitted string, and ledger-write failure is 503. Live development SQL proved service-role insert/select succeeds while anon SELECT, authenticated INSERT, service-role UPDATE, and service-role DELETE each fail with 42501. The attempted tamper/delete did not change the row; the fixture was then removed as postgres and the ledger returned to zero rows.

Current gate: lint pass; TypeScript pass; Vitest 24/24; Next.js production build 37 routes; Playwright 4/4. Agent Browser production checks at 1440x900, 1024x768, 390x844, and 360x800 show meaningful home content, usable navigation/search controls, no horizontal overflow, no framework overlay, and zero console/page errors. Browser sessions, server, screenshots, and database fixtures were cleaned. Live authenticated Next-route and admin-browser journeys remain honestly blocked by P0-005's missing development service secret; a real configured moderation-provider response was intentionally not claimed.

## P1 unread chat state and durable notifications — current evidence

Applied additive development migration: `add_chat_read_state_and_durable_notifications`. Development now has 14 migrations, 11/11 RLS public tables, 45 constraints, and 40 indexes. `chat_room_reads` is authenticated-SELECT-only, has one owner/member policy and one read-ack trigger, and appears exactly once in `supabase_realtime`. `messages` has one durable delivery trigger. The three private trigger functions are SECURITY DEFINER with `search_path=""` and postgres-only execute. Generated types contain the new table, room activity, and notification message link. Schema security lint is clean; the known Pro-only Auth warning remains; performance output is unused-index INFO on the cleaned dataset.

Room creation initializes buyer/seller count zero. Message insert atomically validates membership, increments only the recipient, advances `last_message_at`, and creates exactly one notification keyed by `message_id`; trigger failure rolls back the message. Read acknowledgement atomically sets count zero/server marker and marks matching message notifications read. Direct clients cannot mutate read state. System notification insertion is awaited, unexpected database errors are 500, and optional email outcome is returned instead of detached.

Live RLS/Realtime/reconnect evidence used three temporary confirmed users, one listing, and one room:

| Probe                                  | Result                                                                                            |
| -------------------------------------- | ------------------------------------------------------------------------------------------------- |
| Initial participant state              | Buyer and seller each see one own row/count 0; third sees 0 rows.                                 |
| Direct authenticated read-state update | Denied even for owner; mutation is server-managed.                                                |
| Three buyer messages                   | Seller count 3, buyer count 0, exactly three unique linked notifications.                         |
| Read-state Realtime                    | Seller receives `[3]`; buyer and third receive no seller update.                                  |
| Third/spoofed send                     | RLS denied; no message/count/notification side effect.                                            |
| Privileged nonmember send              | Trigger rejects 23514, proving service writes cannot bypass membership.                           |
| Seller acknowledgement                 | Count 0, read marker present, all three room notifications read.                                  |
| Seller reconnect/reply                 | Persisted 0/all-read; reply makes buyer count 1 and one linked notification.                      |
| Notification ownership                 | Seller cross-mark affects 0 rows; buyer self-mark affects 1.                                      |
| Cross-device read event                | Seller receives notification `read=true` and count `0`; third receives neither.                   |
| Exact room-list relation query         | One room/own read row; buyer/seller keys exactly city/created_at/id/name; listing embed succeeds. |

Production Agent Browser signed in as the temporary buyer on a public page. Desktop navigation exposed `1 unread messages`; mobile exposed `Messages (1)` and `Notifications (1)`. A server acknowledgement changed the live message label 1→0; a seller message changed it 0→1 through protected Postgres Changes. Header/menu scroll width equalled 1440, 1024, 390, and 360 viewports; accessibility names exposed the counts; console/page errors were zero. The protected messages/notifications Next routes remain part of P0-005's external service-secret blocker and were not falsely claimed.

Current gate: lint pass; TypeScript pass; Vitest 17/17; Next.js production build 37 routes; Playwright 4/4. Cleanup counts are zero for temporary Auth users, public users, listing, room, read states, messages, notifications, cleanup jobs, and Storage objects.

## P1 catalog pagination and seller inventory — current evidence

Applied additive development migration: `add_listing_pagination_indexes`. It adds the partial `(price,id)` index for active catalog price traversal and `(seller_id,created_at desc,id desc)` for public active/sold seller inventory. The development catalog now has 13 migrations, 10 RLS-enabled public tables, 40 constraints, and 35 indexes. The security advisor is empty; performance notices are only unused-index INFO on the cleaned development dataset.

Catalog and seller APIs return `{items,nextCursor}` using canonical versioned base64url cursors bound to normalized filters or seller scope. Newest uses `(created_at desc,id desc)`; price-low uses `(price asc,id asc)`; price-high uses `(price desc,id desc)`. Limit+1 detects continuation. Query/category/city/condition/maximum-price/sort are server-side; filter/cursor or catalog/seller scope mismatches return 400. Clients cancel/reset stale first pages, reject stale load-more results, de-duplicate IDs, preserve valid filters in the URL, and normalize malformed deep links.

Live API evidence used two temporary Auth users, five active rows including equal price/timestamp ties, one sold row, one draft, one eligible sold-interaction review, and one concurrent active insert:

| Probe                                         | Result                                                                                   |
| --------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Price-low pages                               | Original IDs in exact `(price,id)` ascending order, once each.                           |
| Price-high pages                              | Original IDs in exact `(price,id)` descending order, once each.                          |
| Newest equal-timestamp tie                    | Higher ID then lower ID, deterministic.                                                  |
| Concurrent newer insert after page one        | Original traversal had no gap/duplicate; newer row correctly remained before the cursor. |
| Category/city/condition/max-price composition | Exactly the three eligible fixtures.                                                     |
| Cursor reused with different filter/scope     | 400 `INVALID_CURSOR`.                                                                    |
| Public seller response                        | Safe keys only: city, createdAt, id, initials, name, rating, reviewCount.                |
| Seller inventory/rating                       | Seven active/sold rows exactly once; draft absent; rating 4 from one eligible review.    |

The first price-high matrix exposed a secondary-ID direction error that duplicated/omitted tied rows; the implementation was corrected to explicit descending ID order and the entire matrix reran successfully. Agent Browser then observed 200 responses for `/api/listings` and `/api/sellers/[id]`, exact filtered catalog order, seven storefront cards including sold and excluding draft, rating text, exposed names for every catalog filter, no console/page errors, and matching viewport/scroll widths at 1440x900, 1024x768, 390x844, and 360x800.

Current gate: lint pass; TypeScript pass; Vitest 17/17; Next.js production build 37 routes; Playwright 4/4. Cleanup counts are zero for both Auth users, public users, listings, reviews, cleanup jobs, and Storage objects.

## P1 image lifecycle — current evidence

Applied additive development migrations:

1. `add_listing_image_cleanup_jobs`
2. `allow_owner_listing_image_selection`
3. `make_cleanup_jobs_service_only_explicit`
4. `deduplicate_listing_image_cleanup_jobs`

The client now revokes every local preview URL, lets sellers remove staged and existing photos, uploads replacements before mutation, and calls an authenticated compensation endpoint after failed create/edit. The server accepts cleanup only for URLs on the configured Supabase host in the authenticated user's exact first-level folder, refuses traversal/nested/query/fragment paths, rechecks live listing references, and drains through the Storage API. A listing image update/delete trigger persists obsolete URLs transactionally in `listing_image_cleanup_jobs`; the queue records failed attempts and is service-only through grants, RLS, and an explicit false client policy. API responses expose `imageCleanupPending` rather than silently hiding a Storage failure.

Live Storage/RLS evidence used two temporary confirmed Auth users, three valid PNG objects, one oversized payload, and shared/obsolete listing references:

| Probe                                             | Result                                                                            |
| ------------------------------------------------- | --------------------------------------------------------------------------------- |
| Owner uploads into own folder                     | Success.                                                                          |
| Other user uploads into owner folder              | Denied.                                                                           |
| Other user deletes owner object                   | Denied/zero rows; public object still fetched successfully.                       |
| Owner deletes own object/batch                    | Success: exactly 1 and 2 deleted entries.                                         |
| `text/plain` upload                               | Denied by bucket MIME restriction.                                                |
| 5 MB + 1 byte PNG upload                          | Denied by bucket size restriction.                                                |
| Other user inserts listing with spoofed seller    | Denied by listing RLS.                                                            |
| Authenticated cleanup-queue SELECT                | Denied; queue is not client-readable.                                             |
| Replace removes one obsolete URL                  | One durable unreferenced job created.                                             |
| Delete listing while another listing shares a URL | Shared URL job created but detected as still referenced; object remained present. |

The first owner-delete probe exposed that the earlier hardening migration had removed Storage SELECT because public asset delivery bypasses RLS; current Supabase removal still requires SELECT plus DELETE. The additive fix grants authenticated SELECT only for the caller's own folder. The corrected full matrix passed. All temporary Auth users, profiles, listings, cleanup jobs, and Storage objects were removed; every final count is zero.

Schema evidence: twelve migrations; 10/10 public tables with RLS; 40 public constraints; 33 public indexes; three owner-scoped Storage policies. The queue trigger exists once, its function is SECURITY DEFINER with `search_path=""`, anon/authenticated have no table privilege, service role has CRUD, duplicate direct-input URLs queue once, and generated TypeScript matches the new table. The final database security advisor returns no findings; performance output remains only expected empty-project unused-index INFO.

Current local/browser gate: lint pass; TypeScript pass; Vitest 14/14; Next.js production build 37 routes; Playwright 4/4. Agent Browser exercised create-photo selection and removal at 390x844: the preview appeared as one blob image, exposed `Remove selected photo 1`, disappeared after removal, and the captured blob URL was revoked. Create/profile surfaces had matching viewport and scroll widths at 1440x900, 1024x768, 390x844, and 360x800, zero console/page errors, and no upload mutation without a session. Live protected Next route calls remain deferred to P0-005 because the development service secret is unavailable; IMG-02/IMG-03 therefore remain Partial rather than Pass despite the implemented backstop and direct Storage evidence.

## P0 public catalog and chat authorization — current evidence

Applied additive development migrations:

1. `fix_chat_room_seller_authorization`
2. `add_chat_room_listing_seller_index`

The first adds the non-exposed `private.user_is_active(uuid)` predicate, rewrites policies that previously tried to read protected `users.banned`, qualifies chat-room ownership columns, and replaces the single-column room/listing foreign key with `(listing_id, seller_id) -> listings(id, seller_id)`. The second adds the advisor-requested covering index. Generated TypeScript confirms the composite relationship.

Current validation:

| Command / probe                        | Result                                                         |
| -------------------------------------- | -------------------------------------------------------------- |
| `npm run lint`                         | Pass, exit 0, no warnings.                                     |
| `npx tsc --noEmit`                     | Pass, exit 0, no diagnostics.                                  |
| `npm test -- --run`                    | Pass: 1 file, 9/9 tests.                                       |
| `npm run build`                        | Pass: Next.js 15.5.19, 37 generated routes.                    |
| `npm run test:e2e`                     | Pass: Chromium 4/4.                                            |
| Direct safe profile read               | 200; only id/name/city/created_at.                             |
| Direct profile email and `*` reads     | 401 with PostgreSQL 42501.                                     |
| Safe listing/seller embed              | 200; seller keys are exactly city/created_at/id/name.          |
| Attempted private RPC through Data API | 404/PGRST202.                                                  |
| Banned-seller probe                    | Profile rows 0 and public listing rows 0; ban reset afterward. |

Direct authenticated role-level chat probes used three temporary users and one active listing. The valid buyer/listing-owner pair inserted successfully. Wrong seller, spoofed buyer ID, self-room, inactive listing, banned buyer, banned seller, missing listing, anonymous insert, and third-party room read all failed. The actual buyer could read its room. A privileged mismatched insert failed the composite foreign key with 23503. Catalog inspection then confirmed zero mismatched rooms, zero compiled tautologies, one composite chat/listing relationship, and the covering index.

Function catalog evidence: `private.user_is_active` is SECURITY DEFINER, stable, strict, configured with `search_path=""`, and executable only by postgres/anon/authenticated. All nine public tables retain RLS. Security advisor reports one external warning: leaked-password protection is disabled; official Supabase documentation states it is available on Pro and above, so the free development project was not upgraded. Performance advisor has only expected new-project unused-index informational notices; the missing foreign-key index warning is resolved.

Production browser evidence with Agent Browser:

| Viewport | Catalog/detail | Seller fixture | Horizontal overflow      | Console/page errors |
| -------- | -------------- | -------------- | ------------------------ | ------------------- |
| 1440x900 | 200            | Rendered       | None (scroll width 1440) | None                |
| 1024x768 | 200            | Rendered       | None (scroll width 1024) | None                |
| 390x844  | 200            | Rendered       | None (scroll width 390)  | None                |
| 360x800  | 200            | Rendered       | None (scroll width 360)  | None                |

The detail API returned 200 and rendered the safe seller name/city at 390x844. Accessibility-tree inspection also found empty accessible names on the catalog search, location, and condition controls; this remains P1-011. A `next dev` run records a React Refresh `unsafe-eval` CSP violation, but a fresh production browser session has zero errors; this remains in P2-005 rather than weakening the production CSP.

Cleanup: all three temporary Auth users were deleted. Cascades were verified: temporary Auth users 0, public profiles 0, listings 0, and chat rooms 0.

## P0 favorite visibility — current evidence

Applied additive development migrations:

1. `secure_favorite_listing_visibility`
2. `restrict_banned_user_favorite_access`

The route now filters privileged favorites through explicit inner relationships, active/sold state, and active seller status; revalidates every nested result before serialization; validates targets before upsert; maps an atomic trigger race to `LISTING_UNAVAILABLE`; and reports unexpected database faults as 500 instead of 401. The database adds a private visibility predicate, a service-role-resistant before-write trigger, and requester/ban/target-aware RLS. Book-card browser verification also raised the heart control above the cover title and made its accessible name reflect save/remove state.

| Probe                            | Result                                                              |
| -------------------------------- | ------------------------------------------------------------------- |
| Buyer direct RLS SELECT          | Exactly active and sold listing IDs; transitioned draft absent.     |
| Other-user SELECT                | Exactly the other user's active favorite; buyer favorites absent.   |
| Valid active insert              | Success.                                                            |
| Draft / locked insert            | 23514 from the atomic target trigger.                               |
| Spoofed `user_id` insert         | 42501 RLS denial.                                                   |
| Anonymous insert                 | 42501 table permission denial.                                      |
| Banned seller                    | Buyer SELECT returns 0; new save fails 23514.                       |
| Banned buyer                     | SELECT returns 0; new save fails 42501.                             |
| Authenticated Data API safe join | 200; only active/sold; seller keys exactly city/created_at/id/name. |
| Draft insert through Data API    | 400 with code 23514.                                                |
| Listing deletion                 | Zero orphan favorite rows after cascade.                            |

Catalog inspection confirms `favorite_listing_is_visible` is stable, strict, SECURITY DEFINER, `search_path=""`, and executable only by postgres/authenticated. The trigger function is SECURITY DEFINER with `search_path=""` and postgres-only execute ACL; its trigger is enabled. The three favorite policies compile with requester, active-user, and target-visibility predicates. Generated TypeScript remains compatible and exposes no private helper. Security advisor remains at the single previously documented Pro-only Auth warning; performance advisor has only expected unused-index informational entries.

Current command/browser gate: lint pass; TypeScript pass; Vitest 10/10; production build 37 routes; Playwright 4/4. At 390x844 the signed-out favorites page has scroll width 390, renders its sign-in state, and logs no console/page error. The corrected catalog save control is clickable and redirects to `/login`; no `/api/favorites` request is sent without a session. The development service secret is still unavailable, so a live protected Next route call is explicitly deferred to P0-005; the database trigger ensures privileged writes cannot bypass the target invariant meanwhile.

Cleanup: three temporary Auth users, their profiles, seven temporary listings, and all temporary favorite rows were removed; all verification counts are zero.

## P0 protected message delivery — current evidence

No migration was required. The active client already used `postgres_changes` on `public.messages`, and official Supabase documentation confirms records from RLS-enabled tables are delivered only when the subscriber may SELECT them. The unauthorised public Broadcast send and dormant Broadcast listener were removed; both clients now use only the published table stream and de-duplicate IDs.

Direct role-level matrix:

| Probe                                  | Result                         |
| -------------------------------------- | ------------------------------ |
| Buyer INSERT as own sender in own room | Success.                       |
| Seller SELECT in the room              | 1 visible message.             |
| Third-user SELECT                      | 0 visible messages.            |
| Third-user INSERT                      | 42501 RLS denial.              |
| Buyer spoofing seller sender ID        | 42501 RLS denial.              |
| Banned buyer INSERT                    | 42501 RLS denial.              |
| Anonymous SELECT                       | 42501 table permission denial. |

Live Realtime evidence used independent authenticated Supabase clients with explicit Realtime access tokens. A buyer insert produced the matching Postgres Changes event for the buyer and seller subscriptions; an already-subscribed third user received zero events. Catalog inspection confirms `public.messages` appears exactly once in `supabase_realtime`, RLS is enabled, and the member SELECT plus active-member INSERT policies are deployed. A source-contract unit test rejects `.channel()`/Broadcast in the send route and requires `postgres_changes` in both chat clients.

Current gate: lint pass; TypeScript pass; Vitest 11/11; Next.js production build 37 routes; Playwright 4/4. Agent Browser rendered the signed-out messages state at 1440x900, 1024x768, 390x844, and 360x800 with matching viewport/scroll widths, no `/api/chat` request, and zero console/page errors. Security advisor remains at the single Pro-only Auth warning; performance advisor remains informational only.

Cleanup: the three temporary Auth users and cascaded profiles, listing, room, and messages were removed; every verification count is zero.

## Static and automated baseline

| Command          | Result       | Exact summary                                                                                                                |
| ---------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| npm run lint     | Pass, exit 0 | ESLint over .js/.ts/.tsx with max-warnings=0; no warnings/output.                                                            |
| npx tsc --noEmit | Pass, exit 0 | No TypeScript diagnostics.                                                                                                   |
| npm test         | Pass, exit 0 | Vitest: 1 file passed, 24 tests passed.                                                                                      |
| npm run build    | Pass, exit 0 | Next.js 15.5.19 compiled in 5.7 s; lint/type/page data passed; 37/37 static pages generated.                                 |
| npm run test:e2e | Pass, exit 0 | Playwright Chromium: 4 tests passed in 2.1 s (browse home/catalog, mobile navigation, safety/user-rights, security headers). |

Production build route evidence:

- 37 route entries generated.
- Shared first-load JavaScript: 102 kB.
- Home: 212 kB first load.
- Catalog: 211 kB.
- Listing detail: 217 kB.
- Favorites: 208 kB.
- Build success does not override runtime API failures below.

## Historical pre-fix four-viewport browser inspection (superseded)

This section preserves the preparation baseline that motivated P0-001. It is superseded by the current evidence above.

The production server was started with the new development project's public URL/publishable key in process environment. At each viewport the script loaded home and catalog to network idle, checked document text/navigation/overflow, captured console/request/HTTP failures, and called /api/listings directly.

| Viewport | Home | Catalog page | Meaningful content/nav | Horizontal overflow | /api/listings | Console/network                                                                             |
| -------- | ---- | ------------ | ---------------------- | ------------------- | ------------- | ------------------------------------------------------------------------------------------- |
| 1440x900 | 200  | 200          | Pass                   | None                | 500           | Two 500 resource errors; catalog navigation RSC request aborted during scripted transition. |
| 1024x768 | 200  | 200          | Pass                   | None                | 500           | Same.                                                                                       |
| 390x844  | 200  | 200          | Pass                   | None                | 500           | Same.                                                                                       |
| 360x800  | 200  | 200          | Pass                   | None                | 500           | Same.                                                                                       |

Rendered title: “BookSwap | Give books a second life”. The existing Playwright mobile navigation test passes. The catalog surface renders its error/empty vocabulary and therefore still contains meaningful text, but the core listing flow fails and is not accepted.

Server-side error captured from the real API:

    code: 42501
    message: permission denied for table users
    hint: Grant the required privileges to the current role with: GRANT SELECT ON public.users TO anon;

Do not follow that hint literally: granting the entire users table would expose email/phone/admin/ban fields. P0-001 requires a safe public profile contract.

## Development backend verification

Target: bookswap-development, ref uibatsbzjswmtdvdrlxj, eu-central-1, ACTIVE_HEALTHY, PostgreSQL 17. The authorized organization was on the free plan and the connector reported zero monthly project cost.

Applied migrations in repository order:

1. init
2. marketplace_upgrade
3. production_hardening
4. security_marketplace_hardening
5. fix_chat_room_seller_authorization
6. add_chat_room_listing_seller_index

Catalog verification:

- 9 public tables, all RLS enabled: users, listings, chat_rooms, messages, reviews, notifications, favorites, reports, privacy_requests.
- 37 public constraints, including the composite chat-room listing/seller foreign key and referenced unique key.
- 30 public indexes including full-text/trigram/filter/listing cursor support, relationship indexes, uniqueness, open-case partial indexes, and the composite chat ownership index.
- Policy counts: users 2; listings 4; chat_rooms 2; messages 2; reviews 2; notifications 2; favorites 3; reports 2; privacy_requests 2; storage.objects 2.
- Grants verified: anon cannot select users.email or users.is_admin; authenticated can update users.phone but not users.email; anon cannot select chat_rooms; authenticated can update notifications.read but not notifications.payload.
- Storage bucket listing-images is public for reads, limited to 5,242,880 bytes, and limited to image/jpeg, image/png, image/webp. Insert/delete policies require the authenticated user ID as the first object path segment.
- supabase_realtime publication contains public.messages and public.notifications.
- on_auth_user_created fires after Auth inserts and calls handle_new_user.
- handle_new_user is SECURITY DEFINER, has empty search_path, and execute ACL only for postgres/service_role.
- TypeScript generation succeeded and contains the same 9 tables and 2 enums as the repository's hand-shaped lib/database.types.ts. The generated file was not written over application code during preparation.
- Supabase security advisor: one external leaked-password protection warning, a Pro-and-above feature on the current free development plan.
- Performance advisor: informational unused-index notices only, expected for a zero-row new project; re-evaluate with representative traffic.

Functional Auth profile evidence:

1. Inserted one temporary development Auth user with a unique audit UUID and safe name metadata.
2. Confirmed public.users contained the same ID/name/email with banned=false and is_admin=false.
3. Deleted the Auth user.
4. Confirmed zero matching public profile rows remained.

No audit test data remains.

## Historical backend defects resolved by this slice

### Chat-room seller authorization

Catalog inspection of the deployed policy returned:

    listing.seller_id = listing.seller_id

The migration source intended listing.seller_id = seller_id, but SQL name resolution bound the unqualified inner reference to the listing relation. The new qualified policy plus composite foreign key resolves this defect; the compiled tautology count is now zero.

### Public profile/API compatibility

Safe column grants existed, but the listing RLS policy itself tried to read protected `users.banned`, causing PostgREST 42501. Moving that predicate behind the private security-definer function restores the existing explicit safe-column embed without widening grants. Current catalog/detail reads return 200.

## Coverage limits

- No service-role/secret key for the new development project is available through the connected tooling or local environment, so protected Next.js route flows were not exercised live.
- Existing E2E tests are signed-out smoke tests and do not cover buyer/seller/admin isolation, RLS adversarial cases, uploads, message membership, unread state, or moderation.
- Only a minimal temporary authorization listing was seeded and removed; search relevance, pagination, query plans, and realistic performance remain unverified.
- Accessibility evidence is foundation/source inspection plus responsive checks, not a complete WCAG audit.
- Deployment, production secrets/domain, monitoring, backups, rollback, and legal operator configuration remain unverified.
